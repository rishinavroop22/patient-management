# Copy this file to .env and fill in your values before running docker compose.

# PostgreSQL credentials (used by auth-service and patient-service databases)
POSTGRES_USER=admin_user
POSTGRES_PASSWORD=change_me

# JWT secret for auth-service (base64-encoded string, e.g. run: openssl rand -base64 32)
JWT_SECRET=change_me
